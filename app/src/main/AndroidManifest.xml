<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.goalsontrack">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GoalsOnTrack">
        <activity android:name=".flashcard.AllWordInCategory"></activity>
        <activity android:name=".flashcard.AddCardToCategory" />
        <activity android:name=".flashcard.FlashcardPractice" />
        <activity android:name=".reminder.ReminderHome" />
        <activity android:name=".moneytracking.MoneytrackingHome" />
        <activity android:name=".todo.TodoHome" />
        <activity android:name=".flashcard.FlashcardHome" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>